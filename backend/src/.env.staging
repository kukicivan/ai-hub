APP_NAME="Backed Laravel"
APP_ENV=local
APP_KEY=base64:SntToiDVB4vs75knXhY7qiA0C8LsFG5p+68TM8lHAJU=
APP_DEBUG=true
APP_TIMEZONE=Europe/Belgrade
APP_URL=https://outsource-team.do-my-booking.com

# Frontend URL for CORS and Sanctum
FRONTEND_URL=https://frontend-react-ten-beta.vercel.app
FRONTEND_URL_2=http://localhost:5173
SANCTUM_STATEFUL_DOMAINS=outsource-team.do-my-booking.com,local.do-my-booking.com,local.do-my-booking.com:5173,frontend-react-ten-beta.vercel.app

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=${DB_DATABASE}
DB_USERNAME=${DB_USERNAME}
DB_PASSWORD=${DB_PASSWORD}

SESSION_DRIVER=cookie
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=.do-my-booking.com
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=none

BROADCAST_CONNECTION=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=file

CACHE_STORE=file
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

# REDIS_CLIENT=null
# REDIS_HOST=null
# REDIS_PASSWORD=null
# REDIS_PORT=null

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher (commented out - not used)
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_HOST=
# PUSHER_PORT=443
# PUSHER_SCHEME=https
# PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
# VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
# VITE_PUSHER_HOST="${PUSHER_HOST}"
# VITE_PUSHER_PORT="${PUSHER_PORT}"
# VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
# VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

JWT_SECRET=r4ryAble74Us5l2pGSLqaTDlbFPMNaQEiFbeHYTa1e3WqREQne99YDWzaxnfZRQy

JWT_ALGO=HS256

JWT_TTL=15
JWT_REFRESH_TTL=20160
JWT_BLACKLIST_ENABLED=true
JWT_BLACKLIST_GRACE_PERIOD=0

# Node API configuration
# If Laravel runs in Docker and Node runs on your host, use:
#   - Mac/Windows: http://host.docker.internal:3001
#   - Linux: http://172.17.0.1:3001 (adjust to your host IP)
# If both are in docker-compose, use the Node service name, e.g. http://node:3001
NODE_API_URL=http://172.17.0.1:3001
NODE_API_TIMEOUT=10

# Messaging Core Node
#   - Additions for config/messaging.php
NODEJS_API_URL=http://localhost:3001
NODEJS_API_TIMEOUT=30

MESSAGING_DEFAULT_CHANNEL=gmail-primary
MESSAGING_SYNC_INTERVAL=300

MESSAGING_AI_ENABLED=true
AI_PROVIDER=openai
AI_MODEL=gpt-4
AI_MAX_TOKENS=500

MESSAGING_MAX_MESSAGES_PER_SYNC=50
MESSAGING_BATCH_SIZE=10

MESSAGING_STORE_ATTACHMENTS=true
MESSAGING_ATTACHMENT_MAX_SIZE=10485760
MESSAGING_CLEANUP_DAYS=90
